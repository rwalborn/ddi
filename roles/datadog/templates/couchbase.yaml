init_config:

instances:
  - server: http://localhost:8091
    user: {{ cb_user }}
    password: {{ cb_pw }}
    # timeout: 10 # Optional timeout to http connections to the API
    # tags:
    #   - optional_tag1
    #   - optional_tag2

