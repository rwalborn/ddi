environment={{ ddi_cpddc_config.environment_name }}
configListeners={{ ddi_cpddc_config.config_listeners|join(',') }}
{# for the future... (per jacob and eric): #}
sfs.configListeners={{ ddi_cpddc_config.config_listeners|join(',') }}
{% if ddi_cpddc_config.env_properties is defined %}
{% for key,value in ddi_cpddc_config.env_properties.items() %}
{{ key }}={{ value }}
{% endfor %}
{% endif %}
